import { IonicVue } from '@ionic/vue';

export default defineNuxtPlugin((nuxtApp) => {
  nuxtApp.vueApp.use(IonicVue, {
    mode: 'ios', // You can change this to 'md' for Material Design
    animated: true
  });
});
